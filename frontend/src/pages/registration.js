const Register= () => {
    console.log('REGISTERING')
    return (<h1>to add to here: a fetch which will be imported as a component and two form components once they're made</h1>)}

export default Register